settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: redis-svc
  path: /code/the-go-programming-language/extra/redis/redis-svc
  commands:
    install:
      status: true
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
